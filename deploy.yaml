import: 
  - recipe/laravel.php

config:
  repository: 'https://github.com/matrocheetos/cardapio-api.git'
  ssh_multiplexing: false

hosts:
  pratofacil.dynv6.net:
    provision_user: ubuntu
    remote_user: deployer
    deploy_path: '~/cardapio-api'
    http_user: caddy

tasks:
  build:
    - run: uptime  

after:
  deploy:failed: deploy:unlock
